server {
    listen 8888;
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;
    }

    location /app/ {
        proxy_pass http://flask:5000/;
    }
}